# Docker

Place local container files here.

Suggested files:

- `Dockerfile.backend`
- `docker-compose.yml`
- `Dockerfile.frontend`

Example local stack:

- backend API
- PostgreSQL
- Redis for queueing
